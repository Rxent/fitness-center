"""Текст пояснительной записки.

Каждая функция section_* принимает объект документа и словарь хелперов
и заполняет одну структурную часть диплома. Хелперы описаны в build.py.

Текст писался с оглядкой на то, что защищать диплом будет студент колледжа
без углублённых знаний программирования, поэтому формулировки максимально
простые.
"""
from __future__ import annotations


def _p(h, doc, text):
    h['paragraph'](doc, text, first_line_indent=h['Cm'](1.25))


def _p_left(h, doc, text):
    h['paragraph'](doc, text, first_line_indent=h['Cm'](0))


# =============================================================================
# ВВЕДЕНИЕ
# =============================================================================
def section_introduction(doc, helpers):
    h = helpers
    h['heading'](doc, 'Введение')

    _p(h, doc,
       'В современных условиях деятельности предприятий сферы услуг '
       'автоматизация внутренних процессов перестала быть роскошью и стала '
       'необходимым элементом конкурентоспособности. Фитнес-центры работают '
       'с большим числом клиентов, которые приобретают абонементы с разным '
       'сроком действия и лимитом посещений, записываются на групповые и '
       'индивидуальные тренировки, общаются с тренерами и ожидают прозрачности '
       'учёта оплаченных услуг.')

    _p(h, doc,
       'Без информационной системы эти задачи решаются вручную: сотрудники '
       'ведут бумажные журналы, тетради учёта и таблицы в Excel, что приводит '
       'к ошибкам при подсчёте оставшихся посещений, потерям записей на '
       'тренировки и невозможности оперативно получать аналитику по '
       'посещаемости и выручке. Ошибки снижают лояльность клиентов, а '
       'отсутствие отчётности лишает руководство инструментов для принятия '
       'управленческих решений.')

    _p(h, doc,
       'Автоматизированная информационная система (АИС) позволяет закрепить '
       'за каждым клиентом личный профиль, хранить список его абонементов, '
       'автоматически контролировать срок действия и число оставшихся '
       'посещений, давать возможность записаться на тренировку через '
       'веб-интерфейс, а руководству — получать отчёты по деятельности '
       'центра в несколько кликов.')

    h['subheading'](doc, 'Актуальность работы')
    _p(h, doc,
       'Актуальность данной выпускной квалификационной работы определяется '
       'тем, что на рынке фитнес-услуг растёт конкуренция, и небольшие '
       'фитнес-центры не всегда могут позволить себе готовое коммерческое '
       'программное обеспечение. Разработка собственной веб-системы даёт '
       'возможность автоматизировать ключевые бизнес-процессы по приемлемой '
       'стоимости, настроить систему строго под внутренние регламенты '
       'предприятия и при необходимости развивать её собственными силами.')

    h['subheading'](doc, 'Цель и задачи работы')
    _p(h, doc,
       'Цель работы — разработать автоматизированную информационную систему '
       'для работы фитнес-центра, которая покрывает основные сценарии работы '
       'администратора, тренера и клиента и упрощает формирование отчётности.')

    _p_left(h, doc, 'Для достижения поставленной цели решены следующие задачи:')
    h['list'](doc, [
        'изучить предметную область — внутренние процессы фитнес-центра',
        'проанализировать существующие программные решения',
        'сформулировать функциональные и нефункциональные требования',
        'спроектировать структуру базы данных (концептуальная и логическая модели)',
        'выбрать технологии и архитектуру приложения',
        'реализовать систему: модели данных, веб-интерфейс, '
        'разграничение доступа по ролям, отчёты',
        'провести ручное тестирование всех основных сценариев',
        'подготовить документацию по проекту',
    ])

    h['subheading'](doc, 'Объект и предмет исследования')
    _p(h, doc,
       'Объект исследования — деятельность фитнес-центра в части '
       'обслуживания клиентов и учёта оказанных услуг. Предмет исследования — '
       'автоматизация этой деятельности с помощью веб-приложения.')

    h['subheading'](doc, 'Практическая значимость')
    _p(h, doc,
       'Разработанная система может быть внедрена в небольшом фитнес-центре '
       'с минимальными затратами на инфраструктуру. Для запуска достаточно '
       'любого компьютера с установленным Python. Интерфейс адаптирован '
       'под разные роли пользователей, что снижает порог обучения персонала.')


# =============================================================================
# 1 АНАЛИТИЧЕСКАЯ ЧАСТЬ
# =============================================================================
def section_analytical(doc, helpers):
    h = helpers
    h['heading'](doc, '1 Аналитическая часть')

    h['subheading'](doc, '1.1 Обзор предметной области')
    _p(h, doc,
       'Фитнес-центр — это предприятие сферы услуг, предоставляющее клиентам '
       'возможность заниматься физической активностью под руководством '
       'тренеров и с использованием специализированного оборудования. '
       'Основными элементами работы фитнес-центра являются: клиенты, '
       'тренерский состав, залы с оборудованием, тренировки (индивидуальные '
       'и групповые), абонементы и платежи.')

    _p(h, doc,
       'Типовой сценарий обслуживания клиента выглядит следующим образом. '
       'Потенциальный клиент приходит в фитнес-центр, знакомится с тарифами '
       'и выбирает подходящий абонемент. Администратор регистрирует нового '
       'клиента в системе учёта, оформляет абонемент и принимает оплату. '
       'Затем клиент получает возможность записываться на тренировки, '
       'проходящие в расписании центра. По факту прихода на тренировку '
       'тренер отмечает посещение в журнале, и с баланса абонемента клиента '
       'списывается одно посещение.')

    _p(h, doc,
       'Руководству фитнес-центра регулярно требуются данные для '
       'принятия управленческих решений: какова посещаемость тренировок, '
       'сколько тренировок провёл каждый тренер за период, какая выручка '
       'получена в разрезе тарифов и способов оплаты. Без автоматизации '
       'эти сведения приходится собирать вручную.')

    _p_left(h, doc,
            'На основании анализа деятельности фитнес-центра выделены '
            'следующие ключевые сущности предметной области:')
    h['list'](doc, [
        'пользователь — лицо, имеющее учётную запись в системе',
        'клиент — пользователь, пользующийся услугами фитнес-центра',
        'тренер — пользователь, проводящий тренировки',
        'администратор — пользователь, управляющий системой',
        'тариф (тарифный план) — условия предоставления услуг '
        '(срок действия, количество посещений, стоимость)',
        'абонемент — конкретный экземпляр тарифа, приобретённый клиентом',
        'платёж — факт оплаты абонемента одним из способов',
        'зал — помещение, где проводятся тренировки',
        'тренировка — занятие в конкретное время с конкретным тренером',
        'запись на тренировку — факт планируемого посещения клиентом тренировки',
    ])

    h['subheading'](doc, '1.2 Обзор существующих решений')
    _p(h, doc,
       'На российском рынке представлено несколько программных продуктов, '
       'автоматизирующих деятельность фитнес-центров. Ниже дан краткий обзор '
       'наиболее распространённых решений.')

    h['table'](doc,
               header=['Продукт', 'Тип', 'Достоинства', 'Недостатки'],
               rows=[
                   ['1С:Фитнес клуб', 'Коммерческий',
                    'Полный функционал, интеграция с 1С:Бухгалтерия',
                    'Высокая стоимость, требует внедрения'],
                   ['Mobifitness', 'Облачный',
                    'Мобильное приложение для клиентов, готовые отчёты',
                    'Абонентская плата, зависимость от провайдера'],
                   ['FitBase', 'Облачный',
                    'Гибкий учёт абонементов, CRM-возможности',
                    'Стоимость растёт с числом клиентов'],
                   ['Excel/бумажные журналы', 'Подручное',
                    'Нет затрат на ПО, привычность',
                    'Высокий риск ошибок, нет отчётности, '
                    'нет доступа клиентам'],
               ],
               caption='Таблица 1.1 – Существующие решения для фитнес-центров')

    _p(h, doc,
       'Существующие коммерческие продукты закрывают большинство задач, но '
       'стоимость их внедрения и сопровождения оказывается высокой для '
       'небольших фитнес-центров. Разрабатываемая система ориентирована '
       'именно на этот сегмент: позволяет запустить её на собственном '
       'сервере без абонентской платы.')

    h['subheading'](doc, '1.3 Постановка задачи')
    _p(h, doc,
       'Требуется разработать веб-приложение АИС фитнес-центра, которое '
       'поддерживает три роли пользователей: администратор, тренер, клиент. '
       'Разграничение доступа реализуется на уровне каждой страницы: '
       'пользователь, не имеющий прав, должен получать ответ сервера 403.')

    _p_left(h, doc, 'Администратор должен иметь возможность:')
    h['list'](doc, [
        'управлять списком клиентов, тренеров, залов и тарифов',
        'создавать и редактировать тренировки',
        'формировать отчёты: посещаемость, выручка, загрузка тренеров',
        'экспортировать отчёты в PDF и Excel',
    ])

    _p_left(h, doc, 'Тренер должен иметь возможность:')
    h['list'](doc, [
        'видеть список своих тренировок',
        'менять статус тренировки (проведена, отменена)',
        'отмечать посещаемость своих клиентов',
    ])

    _p_left(h, doc, 'Клиент должен иметь возможность:')
    h['list'](doc, [
        'зарегистрироваться и войти в систему',
        'просматривать расписание и тренерский состав',
        'приобретать абонементы',
        'записываться на тренировки и отменять записи',
        'видеть свои активные абонементы и историю записей',
    ])

    _p_left(h, doc, 'Нефункциональные требования:')
    h['list'](doc, [
        'система работает через браузер без установки дополнительного ПО',
        'интерфейс адаптирован под экраны разных размеров',
        'пароли хранятся в зашифрованном виде',
        'система запускается одной командой python manage.py runserver',
    ])


# =============================================================================
# 2 ПРОЕКТНАЯ ЧАСТЬ
# =============================================================================
def section_design(doc, helpers):
    h = helpers
    h['heading'](doc, '2 Проектная часть')

    h['subheading'](doc, '2.1 Выбор технологий')
    _p(h, doc,
       'При выборе технологий использовались следующие критерии: '
       'бесплатность и открытый исходный код, наличие документации на '
       'русском языке, достаточная простота для освоения студентом, '
       'широкое распространение в российских командах разработки.')

    h['table'](doc,
               header=['Технология', 'Назначение', 'Обоснование выбора'],
               rows=[
                   ['Python 3.12', 'Язык программирования',
                    'Простой синтаксис, богатая стандартная библиотека'],
                   ['Django 5.1', 'Веб-фреймворк',
                    'Встроенная админ-панель, ORM, миграции, '
                    'система шаблонов, готовая аутентификация'],
                   ['SQLite', 'База данных',
                    'Встроена в Python, не требует отдельной установки, '
                    'подходит для учебного проекта'],
                   ['Bootstrap 5', 'CSS-фреймворк',
                    'Готовые адаптивные компоненты, подключается через CDN'],
                   ['reportlab', 'Генерация PDF',
                    'Поддержка кириллицы, гибкая настройка таблиц'],
                   ['openpyxl', 'Генерация Excel',
                    'Работа с .xlsx без установки Microsoft Office'],
               ],
               caption='Таблица 2.1 – Выбранные технологии')

    h['subheading'](doc, '2.2 Архитектура приложения')
    _p(h, doc,
       'Приложение построено по шаблону MTV (Model — Template — View), '
       'принятому в Django. Модель описывает структуру данных и правила '
       'работы с ними, шаблон отвечает за внешний вид страницы, '
       'представление (view) связывает модели с шаблонами и реализует '
       'бизнес-логику. При обращении пользователя к некоторому URL Django '
       'находит соответствующее ему представление, оно получает данные '
       'через модель, передаёт их в шаблон, и сформированная HTML-страница '
       'отправляется пользователю.')

    _p(h, doc,
       'Код системы разделён на отдельные приложения (Django-приложения), '
       'каждое из которых отвечает за свою часть предметной области. '
       'Такая структура упрощает сопровождение и повторное использование '
       'кода.')

    h['table'](doc,
               header=['Приложение', 'Назначение'],
               rows=[
                   ['users', 'Пользователи, аутентификация, роли'],
                   ['clients', 'Профили клиентов'],
                   ['trainers', 'Профили тренеров и их специализации'],
                   ['subscriptions', 'Тарифы, абонементы, платежи'],
                   ['schedule', 'Залы, тренировки, записи на тренировки'],
                   ['reports', 'Отчёты и экспорт в PDF/Excel'],
                   ['config', 'Общие настройки, корневой URL-маршрутизатор'],
               ],
               caption='Таблица 2.2 – Django-приложения проекта')

    h['subheading'](doc, '2.3 Проектирование базы данных')
    _p(h, doc,
       'База данных спроектирована в соответствии с требованиями предметной '
       'области и содержит 10 основных таблиц. Ниже приведено описание '
       'каждой таблицы с указанием ключевых полей и типа связей.')

    h['table'](doc,
               header=['Таблица', 'Назначение', 'Ключевые поля'],
               rows=[
                   ['User (users_user)', 'Учётная запись пользователя',
                    'id, username, password, role, phone, photo'],
                   ['Client', 'Профиль клиента',
                    'id, user_id (FK→User), date_of_birth, gender, address, '
                    'health_notes, is_active'],
                   ['Trainer', 'Профиль тренера',
                    'id, user_id (FK→User), experience_years, hourly_rate, bio'],
                   ['Specialization', 'Специализации тренеров',
                    'id, name'],
                   ['Trainer_specializations', 'Связь многие-ко-многим',
                    'trainer_id, specialization_id'],
                   ['SubscriptionPlan', 'Тарифный план',
                    'id, name, plan_type, duration_days, price, visits_limit'],
                   ['Subscription', 'Абонемент клиента',
                    'id, client_id, plan_id, start_date, end_date, '
                    'visits_used, status'],
                   ['Payment', 'Платёж за абонемент',
                    'id, subscription_id, amount, method, paid_at'],
                   ['GymRoom', 'Зал для тренировок',
                    'id, name, capacity, description'],
                   ['TrainingClass', 'Тренировка в расписании',
                    'id, name, trainer_id, room_id, start_time, end_time, '
                    'max_participants, status, price'],
                   ['Enrollment', 'Запись клиента на тренировку',
                    'id, client_id, training_class_id, status, enrolled_at'],
               ],
               caption='Таблица 2.3 – Структура базы данных')

    _p(h, doc,
       'Связи между таблицами реализуют следующие правила предметной '
       'области. У каждого клиента есть ровно один пользователь (связь «один '
       'к одному»). У пользователя может быть одновременно активен только '
       'один абонемент (проверка на уровне бизнес-логики). Тренировка '
       'проводится ровно в одном зале одним тренером, но на одну тренировку '
       'может быть записано несколько клиентов (связь «многие ко многим» '
       'через таблицу Enrollment с ограничением уникальности пары '
       '«клиент — тренировка»).')

    h['subheading'](doc, '2.4 Описание ролевой модели')
    _p(h, doc,
       'Разграничение доступа реализовано через поле role в таблице '
       'пользователей. Поле принимает одно из трёх значений: admin, trainer, '
       'client. В коде доступ к защищённым страницам контролируется '
       'декораторами admin_required, trainer_required, client_required. '
       'Пользователь, попытавшийся открыть страницу «не своей» роли, '
       'получает ответ сервера 403 Forbidden. Для пользователей '
       'superuser сохраняется полный доступ ко всем страницам.')


# =============================================================================
# 3 РЕАЛИЗАЦИЯ
# =============================================================================
def section_implementation(doc, helpers):
    h = helpers
    h['heading'](doc, '3 Реализация')

    h['subheading'](doc, '3.1 Структура проекта')
    _p(h, doc,
       'Проект организован в виде стандартного Django-проекта. Ниже '
       'приведена структура каталогов с кратким описанием содержимого.')

    h['code'](doc, """fitness-center/
├── config/          # настройки Django, корневой файл URL
├── users/           # модель User, вход/выход, регистрация
├── clients/         # профиль клиента, список и поиск
├── trainers/        # профили тренеров, специализации
├── subscriptions/   # тарифы, абонементы, платежи
├── schedule/        # залы, тренировки, записи
├── reports/         # отчёты и экспорт PDF/Excel
├── templates/       # HTML-шаблоны
├── docs/            # документация
├── db.sqlite3       # файл базы данных SQLite
├── requirements.txt # зависимости
└── manage.py        # административная утилита Django""")

    h['subheading'](doc, '3.2 Реализация моделей')
    _p(h, doc,
       'Модели описаны на языке Python с помощью ORM Django. Каждому классу '
       'соответствует таблица в базе данных. Django автоматически создаёт '
       'SQL-скрипты миграций при изменении моделей. Ниже приведён фрагмент '
       'модели пользователя с ролью (полный листинг — в приложении А).')

    h['code'](doc, """class User(AbstractUser):
    ROLE_CHOICES = [
        ('admin', 'Администратор'),
        ('trainer', 'Тренер'),
        ('client', 'Клиент'),
    ]
    role = models.CharField(max_length=10, choices=ROLE_CHOICES, default='client')
    phone = models.CharField(max_length=20, blank=True)
    photo = models.ImageField(upload_to='users/', blank=True, null=True)""")

    h['subheading'](doc, '3.3 Автоматическое создание профиля')
    _p(h, doc,
       'Чтобы не забывать создавать профиль клиента или тренера при '
       'регистрации пользователя, используется сигнал post_save: при '
       'сохранении новой записи в таблице User автоматически создаётся '
       'запись в Client или Trainer в зависимости от роли.')

    h['code'](doc, """@receiver(post_save, sender=User)
def create_role_profile(sender, instance, created, **kwargs):
    if not created:
        return
    if instance.role == 'client':
        Client.objects.get_or_create(user=instance)
    elif instance.role == 'trainer':
        Trainer.objects.get_or_create(user=instance)""")

    h['subheading'](doc, '3.4 Разграничение доступа')
    _p(h, doc,
       'Декораторы ролей — это тонкая обёртка над встроенной аутентификацией '
       'Django. Они проверяют, что пользователь вошёл в систему и имеет '
       'нужную роль, и при необходимости возвращают ответ 403 Forbidden.')

    h['code'](doc, """def admin_required(view_func):
    @login_required
    def _wrapped(request, *args, **kwargs):
        if request.user.is_superuser or request.user.role == 'admin':
            return view_func(request, *args, **kwargs)
        return HttpResponseForbidden('Доступ только администратору')
    return _wrapped""")

    h['subheading'](doc, '3.5 Запись на тренировку')
    _p(h, doc,
       'Бизнес-правила записи сосредоточены в отдельной функции '
       '_can_enroll. Перед созданием записи вызывается эта функция, она '
       'проверяет: действительный абонемент у клиента, не прошла ли '
       'тренировка, свободно ли место, не записан ли клиент уже на эту '
       'тренировку. Возвращает пару (можно_записать, причина_отказа). '
       'Полный листинг — в приложении Б.')

    h['subheading'](doc, '3.6 Покупка абонемента')
    _p(h, doc,
       'При покупке абонемента система: проверяет, что у клиента нет '
       'другого активного абонемента; создаёт новую запись в таблице '
       'Subscription с рассчитанной датой окончания (start_date + '
       'duration_days тарифа); создаёт запись в таблице Payment с '
       'указанием способа оплаты (наличные, карта, онлайн). В текущей '
       'реализации оплата является условной — факт оплаты подтверждается '
       'администратором, что упрощает систему и отделяет её от конкретного '
       'платёжного агрегатора. В случае доработки можно подключить '
       'ЮKassa или Stripe без изменения структуры БД.')

    h['subheading'](doc, '3.7 Отметка посещения и списание')
    _p(h, doc,
       'После проведения тренировки тренер заходит в раздел «Мои '
       'тренировки», открывает тренировку и отмечает для каждого '
       'записавшегося клиента факт посещения. При установке статуса '
       '«посетил» система находит активный абонемент клиента и '
       'увеличивает поле visits_used на единицу. Таким образом баланс '
       'абонемента всегда синхронизирован с фактическими посещениями.')

    h['subheading'](doc, '3.8 Отчёты')
    _p(h, doc,
       'Модуль reports содержит три отчёта: посещаемость тренировок, '
       'выручка, загрузка тренеров. Код отчёта разделён на три части. '
       'Файл services.py строит чистый словарь вида '
       '{title, headers, rows, totals} без привязки к способу вывода. '
       'Файл exporters.py превращает этот словарь в Excel (через openpyxl) '
       'или PDF (через reportlab). Файл views.py реализует HTTP-обработчик, '
       'выбирающий, показать HTML-страницу или выдать файл для скачивания.')

    _p(h, doc,
       'Такое разделение позволяет добавлять новые отчёты тремя действиями: '
       'написать функцию в services.py, зарегистрировать view, создать '
       'пустой шаблон, наследующий от общего макета.')

    h['subheading'](doc, '3.9 Интерфейс пользователя')
    _p(h, doc,
       'Все страницы оформлены с помощью Bootstrap 5, подключаемого через '
       'CDN. Это позволяет получить адаптивный интерфейс без написания '
       'собственного CSS. Навигационное меню различается для '
       'разных ролей: клиент видит пункты «Расписание», «Тренеры», '
       '«Абонементы», «Мои тренировки»; тренер дополнительно видит свою '
       'панель; администратор видит также «Клиенты», «Отчёты» и ссылку на '
       'админ-панель Django.')

    _p(h, doc,
       'Скриншоты основных экранов приведены в приложении В.')


# =============================================================================
# 4 ТЕСТИРОВАНИЕ
# =============================================================================
def section_testing(doc, helpers):
    h = helpers
    h['heading'](doc, '4 Тестирование')

    h['subheading'](doc, '4.1 Порядок тестирования')
    _p(h, doc,
       'Тестирование системы выполнялось ручным способом. Все основные '
       'пользовательские сценарии были воспроизведены непосредственно в '
       'работающем приложении через браузер, с использованием демо-учётных '
       'записей администратора, тренера и клиента.')

    h['subheading'](doc, '4.2 Проверенные сценарии')
    _p(h, doc,
       'Ниже перечислены сценарии, проверенные в ходе тестирования, и '
       'ожидаемые результаты. Для каждого сценария фактический результат '
       'совпал с ожидаемым, что подтверждает корректность работы системы.')

    h['table'](doc,
               header=['№', 'Сценарий', 'Ожидаемый результат'],
               rows=[
                   ['1', 'Регистрация нового клиента',
                    'Создаётся пользователь и профиль клиента, '
                    'вход выполняется автоматически'],
                   ['2', 'Вход администратора',
                    'Открывается админ-дашборд со сводкой'],
                   ['3', 'Покупка абонемента клиентом',
                    'Создаётся абонемент со статусом active '
                    'и запись о платеже'],
                   ['4', 'Запись клиента на тренировку',
                    'В расписании появляется запись; '
                    'уменьшается число свободных мест'],
                   ['5', 'Попытка записи без абонемента',
                    'Система выдаёт сообщение об ошибке'],
                   ['6', 'Отметка посещения тренером',
                    'Счётчик visits_used у абонемента клиента '
                    'увеличивается на 1'],
                   ['7', 'Открытие отчёта администратором',
                    'Отчёт формируется и отображается в виде таблицы'],
                   ['8', 'Экспорт отчёта в PDF/Excel',
                    'Файл корректно скачивается, кириллица читается'],
                   ['9', 'Попытка доступа клиента к отчётам',
                    'Возвращается ответ 403 Forbidden'],
               ],
               caption='Таблица 4.1 – Проверенные сценарии')

    _p(h, doc,
       'Все перечисленные сценарии выполнены успешно, ошибок не выявлено. '
       'Система корректно работает со всеми тремя ролями пользователей и '
       'соответствует сформулированным в первой главе требованиям.')


# =============================================================================
# ЗАКЛЮЧЕНИЕ
# =============================================================================
def section_conclusion(doc, helpers):
    h = helpers
    h['heading'](doc, 'Заключение')

    _p(h, doc,
       'В ходе выполнения выпускной квалификационной работы разработана '
       'автоматизированная информационная система для работы фитнес-центра. '
       'Система реализована в виде веб-приложения на языке Python с '
       'использованием фреймворка Django и поддерживает три роли '
       'пользователей: администратор, тренер, клиент.')

    _p_left(h, doc, 'В результате работы выполнены следующие задачи:')
    h['list'](doc, [
        'изучена предметная область «работа фитнес-центра»',
        'проанализированы существующие коммерческие решения',
        'сформулированы функциональные и нефункциональные требования',
        'спроектирована база данных, включающая 10 основных таблиц',
        'реализованы модели, веб-интерфейс, ролевой доступ, '
        'бизнес-правила записи на тренировки и покупки абонементов',
        'реализованы три отчёта с экспортом в форматы PDF и Excel',
        'проведено ручное тестирование всех основных сценариев',
    ])

    _p(h, doc,
       'Разработанная система решает поставленные задачи и может быть '
       'внедрена в небольшом фитнес-центре. Направления развития системы: '
       'интеграция с онлайн-платёжным агрегатором (ЮKassa), мобильное '
       'приложение для клиентов, интеграция с SMS-шлюзом для напоминаний '
       'о тренировках, расширенная аналитика с графиками.')


# =============================================================================
# СПИСОК ИСТОЧНИКОВ
# =============================================================================
def section_references(doc, helpers):
    h = helpers
    h['heading'](doc, 'Список использованных источников')

    refs = [
        'Васильев А. Н. Python на примерах. Практический курс по '
        'программированию. — СПб.: Наука и Техника, 2022. — 432 с.',
        'Гринберг М. Django для начинающих. — М.: ДМК Пресс, 2022. — 384 с.',
        'ГОСТ 7.32–2017. Отчёт о научно-исследовательской работе. '
        'Структура и правила оформления. — М.: Стандартинформ, 2017.',
        'ГОСТ Р 7.0.100–2018. Библиографическая запись. Библиографическое '
        'описание. — М.: Стандартинформ, 2018.',
        'Документация Django [Электронный ресурс]. — URL: '
        'https://docs.djangoproject.com/ru/ (дата обращения: 20.04.2026).',
        'Документация Python [Электронный ресурс]. — URL: '
        'https://docs.python.org/3/ (дата обращения: 20.04.2026).',
        'Документация SQLite [Электронный ресурс]. — URL: '
        'https://www.sqlite.org/docs.html (дата обращения: 22.04.2026).',
        'Документация Bootstrap 5 [Электронный ресурс]. — URL: '
        'https://getbootstrap.com/docs/5.3/ (дата обращения: 22.04.2026).',
        'Документация ReportLab [Электронный ресурс]. — URL: '
        'https://www.reportlab.com/docs/reportlab-userguide.pdf '
        '(дата обращения: 22.04.2026).',
        'Документация OpenPyXL [Электронный ресурс]. — URL: '
        'https://openpyxl.readthedocs.io/ (дата обращения: 22.04.2026).',
        'Полный Д. Дж. Веб-разработка с применением Django. — М.: Вильямс, '
        '2021. — 528 с.',
        'Саммерфилд М. Python на практике. — М.: ДМК Пресс, 2021. — 338 с.',
        'Официальный сайт проекта: https://github.com/Rxent/fitness-center '
        '(дата обращения: 23.04.2026).',
    ]
    for i, ref in enumerate(refs, 1):
        h['paragraph'](doc, f'{i}) {ref}',
                       first_line_indent=h['Cm'](1.25))


# =============================================================================
# ПРИЛОЖЕНИЕ А — ПОЛНЫЙ ЛИСТИНГ ПРОГРАММЫ
# =============================================================================
def section_appendix_a(doc, helpers):
    """Полный листинг исходного кода программы.

    Чтобы листинг всегда соответствовал актуальному коду проекта, файлы
    считываются с диска во время сборки документа. Если какого-то файла
    нет, он просто пропускается.
    """
    import os

    h = helpers
    h['heading'](doc, 'Приложение А')
    h['paragraph'](doc,
                   '(обязательное)',
                   align=h['align_center'],
                   first_line_indent=h['Cm'](0))
    h['paragraph'](doc,
                   'Полный листинг исходного кода программы',
                   align=h['align_center'],
                   first_line_indent=h['Cm'](0))

    _p_left(h, doc,
            'Ниже приведён полный листинг исходного кода разработанной '
            'системы. Листинги сгруппированы по Django-приложениям и '
            'упорядочены так же, как в подразделе 2.2 пояснительной '
            'записки. Файлы миграций, тесты, HTML-шаблоны и статические '
            'ресурсы в приложение не включены: миграции автоматически '
            'генерируются Django по моделям, шаблоны представляют собой '
            'разметку, а не исходный код программы.')

    # Листинги хранятся рядом с content.py в каталоге source_listings/,
    # чтобы диплом был самодостаточным: полный листинг попадал в документ
    # независимо от того, какие файлы есть в рабочей копии репозитория.
    # При необходимости содержимое каталога можно обновить, скопировав
    # актуальные .py-файлы из исходников проекта.
    listings_root = os.path.join(
        os.path.dirname(__file__), 'source_listings',
    )

    files = [
        # Корень проекта и общие настройки.
        ('manage.py',
         'скрипт управления Django-проектом'),
        ('config/settings.py',
         'настройки Django-проекта'),
        ('config/urls.py',
         'корневой URL-маршрутизатор'),

        # Приложение users — пользователи, аутентификация, роли.
        ('users/models.py',
         'модель пользователя (User)'),
        ('users/signals.py',
         'сигнал автосоздания профиля по роли'),
        ('users/decorators.py',
         'декораторы разграничения доступа по ролям'),
        ('users/forms.py',
         'формы регистрации и редактирования профиля'),
        ('users/views.py',
         'представления входа, регистрации и дашбордов'),
        ('users/urls.py',
         'маршруты приложения users'),
        ('users/admin.py',
         'настройка админ-панели для пользователей'),
        ('users/management/commands/seed_demo.py',
         'команда заполнения базы демо-данными'),

        # Приложение clients — клиенты фитнес-центра.
        ('clients/models.py',
         'модель клиента (Client)'),
        ('clients/views.py',
         'представления списка клиентов и карточки клиента'),
        ('clients/urls.py',
         'маршруты приложения clients'),
        ('clients/admin.py',
         'настройка админ-панели для клиентов'),

        # Приложение trainers — тренерский состав.
        ('trainers/models.py',
         'модели тренера и специализации'),
        ('trainers/views.py',
         'представления списка тренеров и карточки тренера'),
        ('trainers/urls.py',
         'маршруты приложения trainers'),
        ('trainers/admin.py',
         'настройка админ-панели для тренеров'),

        # Приложение subscriptions — тарифы, абонементы, платежи.
        ('subscriptions/models.py',
         'модели тарифного плана, абонемента и платежа'),
        ('subscriptions/views.py',
         'представления тарифов и покупки абонементов'),
        ('subscriptions/urls.py',
         'маршруты приложения subscriptions'),
        ('subscriptions/admin.py',
         'настройка админ-панели для абонементов'),

        # Приложение schedule — залы, тренировки, записи на тренировки.
        ('schedule/models.py',
         'модели зала, тренировки и записи на тренировку'),
        ('schedule/views.py',
         'представления расписания, записи и отметки посещений'),
        ('schedule/urls.py',
         'маршруты приложения schedule'),
        ('schedule/admin.py',
         'настройка админ-панели для расписания'),

        # Приложение reports — отчёты.
        ('reports/forms.py',
         'формы выбора периода для отчётов'),
        ('reports/services.py',
         'бизнес-логика построения отчётов'),
        ('reports/views.py',
         'представления отчётов'),
        ('reports/urls.py',
         'маршруты приложения reports'),
    ]

    counter = 0
    for rel_path, description in files:
        full_path = os.path.join(listings_root, rel_path)
        try:
            with open(full_path, 'r', encoding='utf-8') as src:
                source = src.read()
        except (OSError, FileNotFoundError):
            # Если файл по каким-то причинам отсутствует — просто
            # пропускаем, чтобы сборка диплома не падала.
            continue

        counter += 1
        caption = f'Листинг А.{counter} — {rel_path} ({description})'
        h['paragraph'](doc, caption,
                       align=h['align_center'],
                       first_line_indent=h['Cm'](0),
                       space_after=4)
        h['code'](doc, source.rstrip('\n'))
        h['paragraph'](doc, '', first_line_indent=h['Cm'](0))


# =============================================================================
# ПРИЛОЖЕНИЕ Б — ЛИСТИНГ ЗАПИСИ НА ТРЕНИРОВКУ
# =============================================================================
def section_appendix_b(doc, helpers):
    h = helpers
    h['heading'](doc, 'Приложение Б')
    h['paragraph'](doc, '(обязательное)',
                   align=h['align_center'], first_line_indent=h['Cm'](0))
    h['paragraph'](doc, 'Листинг функции проверки правил записи на тренировку',
                   align=h['align_center'], first_line_indent=h['Cm'](0))

    h['code'](doc, """# schedule/views.py (фрагмент)

def _can_enroll(client, training_class, existing_enrollment):
    \"\"\"Вернёт (True, '') если запись разрешена, иначе (False, 'причина').\"\"\"

    # 1. Нельзя записываться дважды на одну и ту же тренировку.
    if existing_enrollment is not None:
        return False, 'Вы уже записаны на эту тренировку'

    # 2. Нельзя записываться на уже прошедшую тренировку.
    if training_class.start_time < timezone.now():
        return False, 'Тренировка уже началась или прошла'

    # 3. Нужен действующий абонемент с непросроченной датой окончания.
    today = timezone.now().date()
    active_sub = (
        Subscription.objects
        .filter(client=client, status='active',
                start_date__lte=today, end_date__gte=today)
        .first()
    )
    if active_sub is None:
        return False, 'У вас нет действующего абонемента'

    # 4. Лимит посещений по абонементу не должен быть исчерпан.
    if (active_sub.plan.visits_limit is not None and
            active_sub.visits_used >= active_sub.plan.visits_limit):
        return False, 'Исчерпан лимит посещений по абонементу'

    # 5. В зале должны быть свободные места.
    current_count = training_class.enrollments.filter(
        status__in=['enrolled', 'attended']).count()
    if current_count >= training_class.max_participants:
        return False, 'Свободных мест нет'

    return True, ''""")


# =============================================================================
# ПРИЛОЖЕНИЕ В — СКРИНШОТЫ
# =============================================================================
def section_appendix_c(doc, helpers):
    import os
    from docx.shared import Cm as DCm

    h = helpers
    h['heading'](doc, 'Приложение В')
    h['paragraph'](doc, '(обязательное)',
                   align=h['align_center'], first_line_indent=h['Cm'](0))
    h['paragraph'](doc, 'Скриншоты работающей системы',
                   align=h['align_center'], first_line_indent=h['Cm'](0))

    screenshots_dir = os.path.join(
        os.path.dirname(__file__), 'screenshots',
    )

    images = [
        ('sc_reports_index.png',
         'Рисунок В.1 – Главная страница раздела «Отчёты»'),
        ('sc_reports_attendance.png',
         'Рисунок В.2 – Отчёт «Посещаемость тренировок»'),
        ('sc_reports_revenue.png',
         'Рисунок В.3 – Отчёт «Выручка» с итогами'),
        ('sc_reports_trainer_load.png',
         'Рисунок В.4 – Отчёт «Загрузка тренеров»'),
        ('sc_reports_pdf.png',
         'Рисунок В.5 – PDF-экспорт отчёта «Выручка»'),
    ]

    for filename, caption in images:
        path = os.path.join(screenshots_dir, filename)
        if not os.path.exists(path):
            continue
        p = doc.add_paragraph()
        p.alignment = h['align_center']
        p.paragraph_format.first_line_indent = h['Cm'](0)
        run = p.add_run()
        run.add_picture(path, width=DCm(15))
        # Подпись под рисунком.
        h['paragraph'](doc, caption,
                       align=h['align_center'],
                       first_line_indent=h['Cm'](0),
                       space_after=8)
